trigger:
  branches:
    include:
      - feature/add-Image
      - feature/add-button
      - master
      - main

    exclude:
      - patch-1
      - patch-2

pr: #manual
  branches:
    include:
      - main

pool:
  vmImage: ubuntu-latest


#  Dev Env
stages:
- stage: DevDeploy
  displayName: Deploy to Dev
  condition: startsWith(variables['Build.SourceBranch'], 'refs/heads/feature/')
  jobs:
  - deployment: DevJob
    displayName: DevJob
    variables:
      - group: Dev-Variable
    environment: Dev
    strategy:
      runOnce:
        deploy:
          steps:
            - checkout: self

            - task: TerraformInstaller@1
              displayName: Install Terraform
              inputs:
                terraformVersion: latest

            - task: TerraformTask@5
              displayName: Terraform Init (Dev)
              inputs:
                provider: azurerm
                command: init
                workingDirectory: $(Work_Dir)
                backendServiceArm: $(devServiceConnection)
                backendAzureRmResourceGroupName: $(devTFStateRG)
                backendAzureRmStorageAccountName: $(devTFStateSA) stgunjandev24794
                backendAzureRmContainerName: $(devTFStateContainer) tfdevgunjan
                backendAzureRmKey: $(devTFStateKey)
                backendAzureRmUseEntraIdForAuthentication: true

            - task: TerraformTask@5
              displayName: Validate (Dev)
              inputs:
                provider: azurerm
                command: validate
                workingDirectory: $(Work_Dir)
                environmentAzureRmOverrideSubscriptionID: exy
            
            - script: |
                curl -s https://raw.githubusercontent.com/terraform-linters/tflint/master/install_linux.sh | bash
                tflint --version
              displayName: 'Install TFLint'

            - script: |
                tflint --init
                tflint --format compact
              workingDirectory: '$(Work_Dir)'
              displayName: 'Run TFLint checks'
                
            - task: tfsec@1
              displayName: TF Security
              inputs:
                version: 'v1.26.0'
                dir: '$(Work_Dir)'
            
            - task: TerraformTask@5
              displayName: Plan (Dev)
              inputs:
                provider: azurerm
                command: plan
                workingDirectory: $(Work_Dir)
                environmentServiceNameAzureRM: $(devServiceConnection)
                commandOptions: "-var-file=dev.tfvars"

            - task: TerraformTask@5
              displayName: Apply (Dev)
              inputs:
                provider: azurerm
                command: apply
                workingDirectory: $(Work_Dir)
                environmentServiceNameAzureRM: $(devServiceConnection)
                commandOptions: "-var-file=dev.tfvars"


#  QA Env
- stage: QADeploy
  displayName: Deploy to QA
  dependsOn: DevDeploy
  condition: and(
      succeeded(),
      startsWith(variables['Build.SourceBranch'], 'refs/heads/feature/')
      )
  jobs:
  - deployment: QAJob
    variables:
      - group: QA-Variable
    environment: QA
    strategy:
      runOnce:
        deploy:
          steps:
            - checkout: self
            - task: TerraformInstaller@1
              displayName: Install Terraform
              inputs:
                terraformVersion: latest

            - task: TerraformTask@5
              displayName: Terraform Init (QA)
              inputs:
                provider: azurerm
                command: init
                workingDirectory: $(Work_Dir)
                backendServiceArm: $(qaServiceConnection)
                backendAzureRmResourceGroupName: $(qaTFStateRG)
                backendAzureRmStorageAccountName: $(qaTFStateSA)
                backendAzureRmContainerName: $(qaTFStateContainer)
                backendAzureRmKey: $(qaTFStateKey)
                backendAzureRmUseEntraIdForAuthentication: true

            - task: TerraformTask@5
              displayName: Validate (QA)
              inputs:
                provider: azurerm
                command: validate
                workingDirectory: $(Work_Dir)

            - task: TerraformTask@5
              displayName: Plan (QA)
              inputs:
                provider: azurerm
                command: plan
                workingDirectory: $(Work_Dir)
                environmentServiceNameAzureRM: $(qaServiceConnection)
                commandOptions: "-var-file=qa.tfvars"

            - task: TerraformTask@5
              displayName: Apply (QA)
              inputs:
                provider: azurerm
                command: apply
                workingDirectory: $(Work_Dir)
                environmentServiceNameAzureRM: $(qaServiceConnection)
                commandOptions: "-var-file=qa.tfvars"


#  Prod Env
- stage: ProdDeploy
  displayName: Deploy to Prod
  dependsOn: [] 
  condition: and(
    succeeded(),
    eq(variables['Build.SourceBranch'], 'refs/heads/main')
   )
  # approval: true   # Enables manual approval
  jobs:
  - deployment: ProdJob
    environment: Prod
    strategy:
      runOnce:
        deploy:
          steps:
            - checkout: self
            - task: TerraformInstaller@1
              displayName: Install Terraform
              inputs:
                terraformVersion: latest

            - task: TerraformTask@5
              displayName: Terraform Init (Prod)
              inputs:
                provider: azurerm
                command: init
                workingDirectory: $(Work_Dir)
                backendServiceArm: $(prodServiceConnection)
                backendAzureRmResourceGroupName: $(prodTFStateRG)
                backendAzureRmStorageAccountName: $(prodTFStateSA)
                backendAzureRmContainerName: $(prodTFStateContainer)
                backendAzureRmKey: $(prodTFStateKey)
                backendAzureRmUseEntraIdForAuthentication: true

            - task: TerraformTask@5
              displayName: Validate (Prod)
              inputs:
                provider: azurerm
                command: validate
                workingDirectory: $(Work_Dir)

            - task: TerraformTask@5
              displayName: Plan (Prod)
              inputs:
                provider: azurerm
                command: plan
                workingDirectory: $(Work_Dir)
                environmentServiceNameAzureRM: $(prodServiceConnection)
                commandOptions: "-var-file=prod.tfvars"

            - task: TerraformTask@5
              displayName: Apply (Prod)
              inputs:
                provider: azurerm
                command: apply
                workingDirectory: $(Work_Dir)
                environmentServiceNameAzureRM: $(prodServiceConnection)
                commandOptions: "-var-file=prod.tfvars"
